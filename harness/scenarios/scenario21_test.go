package scenarios

import "testing"

// Scenario 21 — Subtree group ramp (24.5h ramp test)
//
// 24.5-hour ramp test — always skipped (too long for automated runs).
func TestScenario21_SubtreeGroupRamp(t *testing.T) {
	t.Skip("24.5-hour ramp test — too long for automated runs, always skipped")
}
